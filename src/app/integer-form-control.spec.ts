import { IntegerFormControl } from './integer-form-control';

describe('IntegerFormControl', () => {
  it('should create an instance', () => {
    expect(new IntegerFormControl()).toBeTruthy();
  });
});
