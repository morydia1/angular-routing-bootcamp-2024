import { AuthHttpIntercept } from './auth-http-intercept';

describe('AuthHttpIntercept', () => {
  it('should create an instance', () => {
    expect(new AuthHttpIntercept()).toBeTruthy();
  });
});
